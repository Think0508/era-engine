@ADD_CUSTOM_COM12
SIF !TARGET
	JUMP SIMATTYAUOJISAN
CALL TOUCH_RESET_T(TARGET)
CALL CTRL_CLOTHES_SET(TARGET, "現在衣装の変更_全裸")
CALL CLOTHES_SETTING_TRAIN(TARGET)
CALL CTRL_CLOTHES_SET(MASTER, "現在衣装の変更_全裸")
CALL CLOTHES_SETTING_TRAIN(MASTER)

;直接抄的爱抚环节的数值计算
IF FLAG:70 && !CFLAG:TARGET:344
	BASE:MASTER:TSP -= 20
	SOURCE:快Ｃ = 40
	SOURCE:快Ｂ = 40
	SOURCE:快Ｃ += ABL:PLAYER:指 * 4
	SOURCE:快Ｂ += ABL:PLAYER:指 * 4
	IF TALENT:PLAYER:靈活手指
		TIMES SOURCE:快Ｃ , 1.50
		TIMES SOURCE:快Ｂ , 1.50
	ENDIF
	SIF HETEROSEX(TARGET,PLAYER)
		EXP:PLAYER:(50 + HETEROSEX(TARGET,PLAYER)) += 5
	SIF TALENT:PLAYER:猫舌
		EXP:PLAYER:嗜虐快楽経験 += 1
	EXP:PLAYER:手淫経験 ++
ELSE
	DOWNBASE:体力 += 10
	DOWNBASE:気力 += 40
	DOWNBASE:MASTER:体力 += 20
	DOWNBASE:MASTER:気力 += 20
	SOURCE:情愛 = 50
	SOURCE:性行動 = 60
	SOURCE:露出 = 20
	SOURCE:不潔 = 30
	SOURCE:逸脱 = 20
	SOURCE:反感 = 20
	SIF TALENT:PLAYER:舌頭靈活
		TIMES SOURCE:情愛 , 1.50
	SIF TALENT:自己愛 < 0 || TALENT:抵抗
		SOURCE:鬱屈 += 20
	SIF TALENT:自尊心 > 0
		SOURCE:不潔 *= 2
	CALL ADD_EXP_LESNIAN(5)
	SIF TALENT:PLAYER:猫舌
		EXP:PLAYER:嗜虐快楽経験 += 1
	EXP:PLAYER:手淫経験 ++
ENDIF
TIME += 5
RETURN 1

@ADD_CUSTOM_COM_ABLE12
SIF !TFLAG:100  ;会话实行判定
	RETURN 0
SIF !EQUIP:0
	RETURN 0
SIF CFLAG:うふふ != 1
	RETURN 0
SIF TFLAG:102 == 3 && TFLAG:抱住模式
	RETURN 0
SIF !TARGET
	RETURN 0
RETURN 1

@ADD_CUSTOM_MESSAGE_COM12
PRINTFORMDL %CALLNAME:MASTER%的魔爪贪婪地伸向%CALLNAME:TARGET%衣衫的缝隙…