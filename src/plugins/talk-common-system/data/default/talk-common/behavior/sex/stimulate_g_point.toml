# stimulate_g_point 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：insert/V 链（high_1 示例）→ 条件：premise(high_1)
variable = "stimulate_g_point"
description = "刺激G点指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "{player.name}细致地刺激着{character.name}的敏感点。"

[[entries]]
conditions = "premise(high_1)"
context = "stimulate_g_point指令通用口上 1"
