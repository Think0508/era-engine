# orgasm_edge_on 指令默认通用口上（插件默认层，Layer 1）
variable = "orgasm_edge_on"
description = "绝顶寸止指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"
# ⚠️ 骨架：不搬运 erArk 原文；1 示例 + 1 占位。
[[entries]]
context = "{player.name}强行止住了即将到来的绝顶。"

[[entries]]
context = "绝顶寸止指令通用口上 1"