# 通用指令 — 日常
# 每类指令一个文件，按 type 分
# rest 已迁移到 native-instructions/data/default/instructions/daily.toml（插件默认层）；
# 本 mod 不再覆盖 rest，避免与默认层的有意差异冲突。

[[instructions]]
id = "wait"
label = "待机"
type = "daily"
time_cost = 30
priority = 40
premises = ["NOT_H"]
effects = []