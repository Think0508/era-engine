# back_seat_anal_sex 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：insert/A 链（high_1 示例）→ 条件：premise(high_1)
variable = "back_seat_anal_sex"
description = "背面座位肛交指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "{character.name}背靠{player.name}坐在他腿上。"

[[entries]]
conditions = "premise(high_1)"
context = "back_seat_anal_sex指令通用口上 1"
