# remote_toy_off_in_h 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：SEX/item（high_1 示例）→ 条件：premise(high_1)
variable = "remote_toy_off_in_h"
description = "遥控关闭玩具指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "{player.name}按下遥控，玩具停了下来。"

[[entries]]
conditions = "premise(high_1)"
context = "remote_toy_off_in_h指令通用口上 1"
