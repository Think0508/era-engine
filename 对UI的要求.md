UI设计：
我认为这个游戏引擎的UI设计也应是解耦的，这方便后续可扩展性（比如，某mod完全可以换一套UI的显示方式）。每一部分都不要硬写死在里面，方便替换、维护、扩展。
同一套UI，在PC端与手机端的排版方式也应有不同优化（但本质是同一套的适配优化）。

我预计内置的原生UI有两套设计，支持切换：
- era经典
- 现代

由于UI是为了满足“类era”的游玩与后续扩展的需要，所以我们可以先看一个比较标准的era的主界面是怎样的，如下从上到下：
```
时间、天气等
地点
人物角色栏（多个，默认一定有一个焦点，可点击改变焦点，焦点就是选中角色）
选中角色的简要说明与情况
以下栏一直到指令栏都可以各自独立折叠：
Status（原生默认3行，玩家、选中角色、选中角色的情绪与理性、特殊情况。前两行参考era的UI，显示体力、气力、精的条与数值，后面可以加状态与扩展）
Parameter（状态参数，这个是每天每人无特殊设定的话都会默认归零的。因为它表示一组或多组临时身体及情绪心理状态的数值。互动会让这个变化）
Look（可以开关立绘、开关设定）
	- 选中角色的上身下身等部位的穿着、及饰品等扩展
	- 下面就是立绘，这部分是本地点所有角色的立绘按标题栏顺序排列（立绘上方也有名字，名字与立绘都像标题栏中的名字一样支持点击切换焦点等特性。居中，可以自动换行）；立绘不一定只有一张图，可能旁边有差分、部位细节放大图、部位截面图等，都是附属于同一个角色的，这个要具备方便的维护与可扩展性
指令栏（就在立绘下方。可能是即时执行的指令，也可能是选中/未选中时会变色的可选项，如自动时停移动 选中了，就变蓝，那么在时停的TSP未耗尽时，去哪儿都会瞬移，不耗时只耗对应TSP）
	- 分Act_COM（游戏内比较具体的行动指令）与Ext_COM（各种相对超游、偏系统的指令与设定）
	- 可以在指令栏的栏上显示/隐藏对应的某批指令，比如显示/隐藏H指令，达到按个人要求过滤的效果。指令栏显示的是过滤后的总和（比如只显示H与家务，在Act_COM中就只有这两类指令，先H然后换行家务这样排列）
	- 指令栏的栏上支持收藏指令，作为快捷指令。
```

在标准的era中，界面被设计为文字信息的一部分，全屏是一个滚动的类似文字命令行的画面。在界面选择指令后，就会向下滚动在最新行显示内容，执行完后又会视情况回到主界面或到达对应界面。
角色面板等，是由“能力显示”“能力显示（主角）”指令触发的。
以上这些就是era的UI设计，可以参看C:\Users\d\Documents\era-engine\供参考的UI eraTW的UI（它是era引擎做的老游戏）目录中的图片。

现代UI怎么做呢？
首先功能性不能缺失，也不能大改（因为要便利、好维护），还要提供像上面一样的足够扩展性（比如如果有mod或插件想改、想override、想在某间隙加一句话、或换顺序，应该在代码上比较清晰好换）。
我决定整体上参考DOL的UI排列方式（DOL的UI在C:\Users\d\Documents\era-engine\供参考的UI 一个有点类似era的游戏DOL的UI 这个目录里有图片）：
- 要有可以侧滑出的侧栏，侧栏可以调宽度。
	- 最上方是角色图片（头图，可在选项中显示/隐藏），下面应该是一个两择的选项卡，分别对应当前选中的角色/主角，如果当前没有选中的角色（意味着该地点没人），那就只是主角。以下内容都对应此人。
		- 最上方图片是角色头图/立绘/……总之是设定好的可以在这里当头像的图（但不影响主界面的立绘部分依旧显示）。无图则这里无（此时也不需要占位）
	- 下方一栏，是简写的时间（只当天时间）、天气、金钱数等。
	- 再下方，是（可）分为多栏的Parameter（状态参数）。简单来说，就是在从引擎获得（各插件、mod声明注册的）需要在Parameter栏显示的状态参数（有的也显示但不想在这里显示，比如eraTW的酒意、困意等就是）后，把它们按顺序排在这里。参考DOL的排法，但默认情况下文字简单（类似eraTW那样，只有快CLV 0这种写法），配一个进度条。这个进度条很短，仅示意。如果侧栏被拉宽，显示的列就多一些，比如侧栏能一行显示4个、5个参数；侧栏被缩窄，一行就只能显示1个参数与它的进度条。
- 再下方，角色面板与各种设置，就放在可以侧滑出的侧栏的下方按钮里，包含以下内容：
	- 属性（核心属性、各种特征、技能、性技能。还可以对标era角色面板里的 服装&能力 模块。此外这里也有 素质，与后面特质/素质有联系——这里的素质是已取得的素质的罗列，后面的特质素质部分则应该是类似“该角色可刷的素质已取得/未取得但有什么条件/无法达成”等）
	- 社交（声望、人际关系、关系中的特定角色。可以对标era角色面板里的 个人情报 模块。era里的个人好恶感觉也可以融在这里，不叫社交，叫别的名字，这样角色比较丰富可扩展）
	- 特质（类似天赋，有点像 陷落状态 里的 素质，不过独立出来是有道理的）
	- 日志（类似era的日历，以及最近的备忘。这里的日志与代码开发角度的日志不是一回事，是类似“主角/江湖大事志”的东西）
	- 统计（各种经验数字，感觉可以与属性区分开，做更参考era的面板。对标era角色面板里的 经验&宝珠）
	- 成就（类似era每日菜单中的实绩解除）
	- 选项、作弊、存档（不用说。先只做最小化实现，细节放todolist记下）
- 侧栏中这些功能哪些需要放在同一按钮的不同选项卡里，一共6个按钮（支持后续修改与扩展）：
	- 属性素质：属性肯定值得一个按钮。特质（类似天赋）我认为可以作为属性的一个选项卡。
	- 个人情报：个人情报（社交）与个人好恶，这些可以统合为“个人情报”，放在两个选项卡中，就像把era的那两部分直接挪过来
	- 日志统计：日志、统计、成就，我认为可以放在同一个按钮里，作为3个或4个选项卡（统计可以分统计与额外统计，把一些扩展的、很细很杂的放在额外统计里）。
	- 选项
	- 作弊
	- 存档

{附上所参考的DOL的侧栏按钮说明：
属性（核心属性、各种特征、技能、性技能）
社交（声望、人际关系、关系中的特定角色）
特质（类似天赋）
日志（类似era的日历，以及最近的备忘）
统计（各种经验数字，感觉可以做更参考era的面板）
成就（类似era每日菜单中的实绩解除）
选项、作弊、存档（不用说。先只做最小化实现，细节放todolist记下）
选项中可以开启屏幕小键盘，用来方便数字式地输入指令（相当于同时也能使用的键盘，是鼠标交互之外另一种交互方式）}

- 主体部分，要保留的，按顺序如下：
```
时间、天气等
地点
人物角色栏（多个，默认一定有一个焦点，可点击改变焦点，焦点就是选中角色）
选中角色的简要说明与情况（参考era的UI）
以下栏一直到指令栏都可以各自独立折叠：
Status（要保留，原生默认3行，玩家、选中角色、选中角色的情绪与理性、特殊情况。前两行参考era的UI，显示体力、气力、精的条与数值，后面可以加状态与扩展）
Parameter（选这套现代UI则默认关闭，可以在选项中开启。与侧栏的是同一个东西，但显示上互不影响，都能在选项中各自独立开关）
Look（可以开关立绘、开关设定）
	- 选中角色的上身下身等部位的穿着、及饰品等扩展
	- 下面就是立绘，这部分是本地点所有角色的立绘按标题栏顺序排列（立绘上方也有名字，名字与立绘都像标题栏中的名字一样支持点击切换焦点等特性。居中，可以自动换行）；立绘不一定只有一张图，可能旁边有差分、部位细节放大图、部位截面图等，都是附属于同一个角色的，这个要具备方便的维护与可扩展性
指令栏（就在立绘下方。可能是即时执行的指令，也可能是选中/未选中时会变色的可选项，如自动时停移动 选中了，就变蓝，那么在时停的TSP未耗尽时，去哪儿都会瞬移，不耗时只耗对应TSP）
	- 分Act_COM（游戏内比较具体的行动指令）与Ext_COM（各种相对超游、偏系统的指令与设定）
	- 可以在指令栏的栏上显示/隐藏对应的某批指令，比如显示/隐藏H指令，达到按个人要求过滤的效果。指令栏显示的是过滤后的总和（比如只显示H与家务，在Act_COM中就只有这两类指令，先H然后换行家务这样排列）
	- 指令栏的栏上支持收藏指令，作为快捷指令。
```
	- 说白了，就是Parameter可以放在侧栏了，省点显示空间。
- 还有一个甜点优化，就是可以在选项里设个开关，开启后，长按标题栏的角色名/长按立绘上的角色名/长按立绘（本质一样），会弹出当前与长按的角色相关的指令可选（开这个选项后，指令栏与角色相关的指令就不显示了）。这是为了防止指令太多挤占显示空间，本质上是给这些指令换了个地方分开显示。不过做这个功能时要注意，弹出的指令也应兼容各种“输入指令”的代码；以及它默认是先切换了选中角色。
- 不算侧栏的话，主显示屏幕上，我希望的游玩体验都是类似era的。即像一个滚动的命令行一样，只是现在这个“命令行”更现代、支持各种新技术与视觉效果，但它的外观依然是“从最新一行滚动出来的”。（这个不用写得特别死以至于不好维护，因为有可能我后期把它变成“每次清屏后显示新的”。要留出以后我可能改的余地，且全局统一显示逻辑，除非被Override）
	- 口上、演出等各种东西，默认原生就是从最新一行滚动出来；
	- 如果要做复杂的东西，推荐“滚动出最新一屏，在屏中可以自由地引入各种面板、各种自己搞的UI、排版，各种自己设计的特性，包括在当前一屏持续交互之类，直到指令离开这个持续交互”，而这些是mod或插件自己实现的，原生只是留出方便维护与这样扩展的清晰代码结构；
	- 各种对话分支等，涉及到选择、输入的，也都是在最新一行出现。可以鼠标点对应选择（移动到上面时高亮），也可以键盘输入对应的y/n 编号数字 或有默认的情况下默认，回车等等。

以下是两种UI布局都可以用的：

- 无论是era经典还是现代UI，都可以在选项中开启后，在右边缘做一个始终在的屏幕小键盘，就像DOL那样，效果是：
1. 显示1-9的数字，按下对应数字（比如399），回车/按屏幕小键盘的确认键，就是输入对应指令（该指令需要是当前可用的，与鼠标点击同效）
2. 玩家可以在这里放快捷指令（支持起别名），会显示为一个个键（键的外观就是别名，无别名的话默认就是指令第一个字），点一下即时执行（同样需要是当前可用的）
3. 前两个功能，都可以在选项的“屏幕小键盘”栏各自独立开启关闭。比如只留数字-确认，或只留快捷指令）

- 战斗等的UI，本质上就是主UI（mod扩展后可能override），只是指令栏换了一套组对应指令。参考era的UI图片中的H场景，指令栏的分批、内容都不一样了。
- 各种插件、系统的UI，则是参考era的UI图片里调合（炼药）的UI或角色面板的UI，即不带主UI、最新一行滚动出的一个新的UI。此外能复用的话，当然比较容易少出错。不过这里，通用UI只需要做原生的那些最小化实现，mod的话打个样留出可扩展性就行

- 地图的UI有些特别。它同样是最新滚动出来（或后期可能改为清屏后新出现）的，但它可以有很多种：
	- 目前的文字节点式地图
	- ASCII地图（是否有现成的生成它的库能用？）
	- 其他可视化地图（包括后期我很可能上的美术资源）
	- 同时，地图可以平铺，可以多层，可以有单独的该UI配合的扩展系统与指令、指令栏等……后续这些的话应该留出mod的扩展性与维护性
	- 点击地图上的对应地点，就会移动过去
	- 可以在地点后面显示移动过去消耗的时间