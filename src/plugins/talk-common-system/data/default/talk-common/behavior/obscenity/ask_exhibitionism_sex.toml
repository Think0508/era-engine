# ask_exhibitionism_sex 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：邀请露出（high_1 示例）→ 条件：premise(high_1)
variable = "ask_exhibitionism_sex"
description = "邀请露出指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "{player.name}邀请{character.name}一起到人前体验露出。"

[[entries]]
conditions = "premise(high_1)"
context = "ask_exhibitionism_sex指令通用口上 1"