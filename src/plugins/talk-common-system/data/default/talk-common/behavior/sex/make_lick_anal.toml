# make_lick_anal 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：high_1 → 条件：premise(high_1)
variable = "make_lick_anal"
description = "命令对方舔自己肛门指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 高权重基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "{player.name}将{character.name}的脸按在了自己的屁股上，强迫{character.name}的小嘴为自己的后庭进行服务。"

[[entries]]
conditions = "premise(high_1)"
context = "命令对方舔自己肛门指令通用口上 1"