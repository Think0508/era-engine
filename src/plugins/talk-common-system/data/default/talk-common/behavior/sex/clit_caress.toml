# clit_caress 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：high_1 → 条件：premise(high_1)
variable = "clit_caress"
description = "阴蒂爱抚指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 高权重基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "{player.name}爱抚着{character.name}的阴蒂，小心翼翼地剥开上面的外皮，露出里面的粉红色部分，用手指轻轻地揉捏。"

[[entries]]
conditions = "premise(high_1)"
context = "阴蒂爱抚指令通用口上 1"