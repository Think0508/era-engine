# normal_cervix_sex 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：insert/宫颈链（high_1 示例）→ 条件：premise(high_1)
variable = "normal_cervix_sex"
description = "正常位子宫口姦指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "正常位下，{player.name}缓慢顶弄{character.name}的子宫口。"

[[entries]]
conditions = "premise(high_1)"
context = "normal_cervix_sex指令通用口上 1"
