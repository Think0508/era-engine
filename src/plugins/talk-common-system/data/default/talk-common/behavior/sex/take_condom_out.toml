# take_condom_out 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：SEX/item（high_1 示例）→ 条件：premise(high_1)
variable = "take_condom_out"
description = "摘掉避孕套指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "{player.name}取下避孕套放到一旁。"

[[entries]]
conditions = "premise(high_1)"
context = "take_condom_out指令通用口上 1"
