# wait_5_min_in_h 指令默认通用口上（插件默认层，Layer 1）
variable = "wait_5_min_in_h"
description = "等待五分钟指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"
# ⚠️ 骨架：不搬运 erArk 原文；1 示例 + 1 占位。
[[entries]]
context = "在 H 中静静等待了五分钟。"

[[entries]]
context = "等待五分钟指令通用口上 1"