# clean_blowjob 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：SEX/wait_upon（high_1 示例）→ 条件：premise(high_1)
variable = "clean_blowjob"
description = "清洁口交指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "{character.name}仔细舔净{player.name}阴茎上残留的痕迹。"

[[entries]]
conditions = "premise(high_1)"
context = "clean_blowjob指令通用口上 1"
