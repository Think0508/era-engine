# external_womb_massage 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：high_1 → 条件：premise(high_1)
variable = "external_womb_massage"
description = "体外子宫按摩指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 高权重基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "{player.name}的手放在{character.name}的肚子上，摸索到子宫的位置之后，开始用适当的力度隔着肚皮按摩体内的子宫。"

[[entries]]
conditions = "premise(high_1)"
context = "体外子宫按摩指令通用口上 1"