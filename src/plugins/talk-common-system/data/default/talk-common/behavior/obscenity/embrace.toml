# embrace 指令默认通用口上（插件默认层，Layer 1）
variable = "embrace"
description = "拥抱指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"
# ⚠️ 骨架：不搬运 erArk 原文；1 示例 + 1 占位。
[[entries]]
context = "{player.name}轻轻抱住了{character.name}。"

[[entries]]
context = "拥抱指令通用口上 1"