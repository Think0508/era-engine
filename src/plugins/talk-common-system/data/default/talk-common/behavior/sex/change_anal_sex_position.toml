# change_anal_sex_position 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：insert/A 链（high_1 示例）→ 条件：premise(high_1)
variable = "change_anal_sex_position"
description = "换肛交体位指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "两人调整姿势，换了个更顺的肛交体位。"

[[entries]]
conditions = "premise(high_1)"
context = "change_anal_sex_position指令通用口上 1"
