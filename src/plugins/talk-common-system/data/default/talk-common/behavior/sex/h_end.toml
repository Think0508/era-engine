# h_end 指令默认通用口上（插件默认层，Layer 1）
variable = "h_end"
description = "结束H指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"
# ⚠️ 骨架：不搬运 erArk 原文；1 示例 + 1 占位。
[[entries]]
context = "{player.name}结束了与{character.name}的H。"

[[entries]]
context = "结束H指令通用口上 1"