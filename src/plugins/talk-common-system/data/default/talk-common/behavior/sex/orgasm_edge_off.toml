# orgasm_edge_off 指令默认通用口上（插件默认层，Layer 1）
variable = "orgasm_edge_off"
description = "绝顶解放指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"
# ⚠️ 骨架：不搬运 erArk 原文；1 示例 + 1 占位。
[[entries]]
context = "{player.name}解除了寸止，让快感直冲顶峰。"

[[entries]]
context = "绝顶解放指令通用口上 1"