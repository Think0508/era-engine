# kiss 指令默认通用口上（插件默认层，Layer 1）
variable = "kiss"
description = "亲吻指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"
# ⚠️ 骨架：不搬运 erArk 原文；1 示例 + 1 占位。
[[entries]]
conditions = "premise(high_1)"
context = "{player.name}轻轻吻上{character.name}的唇。"

[[entries]]
conditions = "premise(high_1)"
context = "亲吻指令通用口上 1"