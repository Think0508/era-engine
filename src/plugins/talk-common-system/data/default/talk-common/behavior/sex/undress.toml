# undress 指令默认通用口上（插件默认层，Layer 1）
variable = "undress"
description = "脱衣服指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"
# ⚠️ 骨架：不搬运 erArk 原文；1 示例 + 1 占位。
[[entries]]
context = "{player.name}将{character.name}身上的衣物脱下。"

[[entries]]
context = "脱衣服指令通用口上 1"