# womb_os_caress 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：insert/V 链（high_1 示例）→ 条件：premise(high_1)
variable = "womb_os_caress"
description = "玩弄子宫口指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "{player.name}缓缓抵住{character.name}的子宫口轻轻研磨。"

[[entries]]
conditions = "premise(high_1)"
context = "womb_os_caress指令通用口上 1"
