# raise_skirt 指令默认通用口上（插件默认层，Layer 1）
variable = "raise_skirt"
description = "掀起裙子指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"
# ⚠️ 骨架：不搬运 erArk 原文；1 示例 + 1 占位。
[[entries]]
context = "{player.name}伸手掀起{character.name}的裙子。"

[[entries]]
context = "掀起裙子指令通用口上 1"