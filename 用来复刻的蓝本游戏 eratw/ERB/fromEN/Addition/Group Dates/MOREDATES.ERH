#DIM SAVEDATA MOREDATE

;Global variables for group date stuff since FLAG is one-dimensional
#DIM Datees, 200 ; Equivalent to FLAG:デート相手 - Ideally this would be CHARANUM but also that might be excessive
#DIM DatesPrevLike, 200 ; Equivalent to TFLAG:デート前好感度 - what is this even used for? not sure 
#DIM NumberOfDates = 0
#DIM HouseInvitationStatus = 0 ; Flag for knowing if you're getting invited to a house at the start/during a date (0) or at the end (1), matters for removing date status