# make_masturebate 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 原文（原 CSV 为占位句）；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：high_1 → 条件：premise(high_1)
variable = "make_masturebate"
description = "命令对方自慰指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 高权重基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "{player.name}命令{character.name}自己抚慰身体，{character.name}羞涩地照做了。"

[[entries]]
conditions = "premise(high_1)"
context = "命令对方自慰指令通用口上 1"