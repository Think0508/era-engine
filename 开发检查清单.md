# 开发检查清单 — era-engine

此文件是所有开发工作的事前约束 + 事后自检清单。
**任何阶段开始前必须读此文件**，完成后逐条自审。

---

## 前置约束（在任何阶段开始前必须确认）

### 架构铁律（违反即致命）
- [ ] 内核 `src/core/` 无任何具体玩法/世界观/美术内容
- [ ] 内核不预设任何属性名/字段名/分类名
- [ ] 插件之间无直接 import（extends 走引擎中介，不走 import）
- [ ] 所有颜色/字体/圆角/间距走 CSS 主题变量，无硬编码
- [ ] 核心游戏界面全定制，无 Naive UI 痕迹
- [ ] 错误报文件+行号+原因，无静默失败

### 数据铁律（违反即后期爆炸）
- [ ] 只用 TOML，无 YAML 代码或依赖
- [ ] 95% 配置走 TOML，复杂逻辑才用 JS 钩子
- [ ] 属性不写死在代码里——插件声明 `required_attributes`，模组 `bindings.toml` 做映射
- [ ] 条件字段动态收集（见 AGENTS.md 第21节），不硬编码字段列表
- [ ] 条件表达式只支持 `> < >= <= == != && || !` 和括号，不写迷你脚本
- [ ] ID引用（exit/template/character reference）加载时全量校验

### 命名铁律
- [ ] 代码标识符英文/拼音，无中文
- [ ] 事件名格式：`领域:动作`
- [ ] 非标准事件加插件前缀
- [ ] 素材文件英文/拼音

### 响应式铁律
- [ ] PC左右分栏，<768px 上下堆叠
- [ ] 按钮最小 44px 触摸目标
- [ ] 所有尺寸用相对单位或CSS变量

---

## 阶段1：项目初始化（自查项）

完成 AGENTS.md「三步走启动」后逐条检查：

- [ ] `npm run dev` 无报错，浏览器正常显示
- [ ] `src/core/`、`src/plugins/`、`src/ui/`、`mods/` 目录完整
- [ ] Tailwind CSS 样式生效（在浏览器里写一个带 Tailwind class 的 div 验证）
- [ ] `@/` 路径别名生效（import 一个 src 下的文件验证）
- [ ] TOML 文件可 import（`import.meta.glob('/mods/**/*.toml', { as: 'raw' })` 不报错）
- [ ] `era-engine.config.toml` 已创建（内容 `active_mod = ""`）
- [ ] `git init` 已提交，`.gitignore` 含 `node_modules/`、`dist/`
- [ ] 响应式断点基础配置完成（Tailwind config 含 md breakpoint）

---

## 阶段2：核心数据系统（自查项）

### 模组加载器 (mod-loader.ts)
- [ ] `import.meta.glob` 正确扫描 `/mods/**/*.toml`
- [ ] 可加载指定模组的所有 TOML 文件
- [ ] 可按路径区分不同模组，不交叉加载
- [ ] `meta.toml` 解析正确（id/name/version/dependencies）
- [ ] 依赖检查：缺失依赖精准报错（含插件名+版本要求）

### 模板继承 (template.ts)
- [ ] 多级继承正确：A→B→C 深合并
- [ ] 对象深合并：子 key 覆盖父，父独有保留
- [ ] 数组子替换父（不追加）
- [ ] `null` 字段移除
- [ ] 循环继承检测：A→B→C→A 精准报错（文件+行号+继承链）
- [ ] 父模板不存在时精准报错

### 实体系统 (entity-system.ts)
- [ ] 按类型+ID 存取（如 `get('character', 'linghuchong')`）
- [ ] 同类型内 ID 重复精准报错
- [ ] 未定义的实体类型报错
- [ ] 跨类型同名 ID 不冲突

### 数据校验
- [ ] TOML 解析错误报文件名+行号
- [ ] 引用不存在的模板/角色 ID 报错
- [ ] `bindings.toml` 正确解析，缺少绑定精准报错（含插件名+需要的属性名）
- [ ] `era-engine.config.toml` 正确读取活跃模组

### 测试
- [ ] 提供最小测试模组（3个角色 + 2个地点 + 模板继承链）
- [ ] 模板继承有单元测试
- [ ] 实体系统有单元测试
- [ ] 属性绑定（bindings）有单元测试
- [ ] `era-engine.config.toml` 能正确切换不同的测试模组

---

## 阶段3：条件系统 + 事件总线（自查项）

### 条件解析 (condition.ts)
- [ ] 解析 `> < >= <= == != && || !` 和括号
- [ ] 从 GameContext 中取值求值
- [ ] 字符串值用双引号包裹（如 `location.id == "tavern"`）
- [ ] **无算术、函数、正则**
- [ ] 非法字段报错（文件名+行号+不存在的字段）
- [ ] 表达式语法错误精准报错

### 事件总线 (event-bus.ts)
- [ ] on/off/once/emit 基本方法
- [ ] 事件命名检查：标准事件格式 `domain:action`
- [ ] **非标准事件无前缀 → 警告**
- [ ] 每个 handler try/catch 包裹，报错不阻断
- [ ] 参数传递正确

### 条件注册 (condition-registry.ts)
- [ ] 插件可通过 `plugin.toml` 注册 condition_fields
- [ ] 引擎启动时自动汇总所有字段
- [ ] 自动生成 `可用条件属性手册.md`
- [ ] **字段来自扫描，无硬编码列表**
- [ ] 换模组后手册自动不同

### 游戏上下文 (game-context.ts)
- [ ] 统一状态容器，供条件求值
- [ ] 数据变化时条件可拿到最新值

### 测试
- [ ] 条件解析有单元测试（覆盖所有运算符+括号分组+错误情况）
- [ ] 事件总线有单元测试
- [ ] 条件注册有单元测试

---

## 阶段4：公共API + 插件生命周期（自查项）

### 插件生命周期
- [ ] `plugin.toml` 格式完整：至少含 id/name/version
- [ ] onLoad / onEnable / onDisable / onUnload 四生命周期
- [ ] 禁用插件后数据无残留、UI无残留
- [ ] 插件加载错误：禁用该插件+弹警告，不死锁启动
- [ ] depends 版本号校验（semver）：不匹配精准报错
- [ ] extends 链接正确建立（见下一条）

### 插件继承
- [ ] **子插件不 import 父插件源文件**
- [ ] `ctx.parent.api.xxx` 可访问父插件公共API
- [ ] 子插件可覆盖父插件的事件 handler
- [ ] 子插件自动继承父插件的 required_attributes
- [ ] 不支持多继承（extends 最多一个）
- [ ] 循环继承检测精准报错

### 公共API
- [ ] `ctx.api.register(name, methods)` 注册
- [ ] `ctx.api.call(name, method, ...args)` 调用
- [ ] 同命名空间重复注册 → 报错
- [ ] 调用不存在API → 报错（含插件名+方法名）
- [ ] 引擎核心 API（engine.getEntity、engine.bindings.get/set、engine.save/load）可用

### 同类型插件统一接口
- [ ] 所有战斗插件发出/响应同一套标准事件（combat:start/combat:turn/combat:end）
- [ ] 类似标准事件清单为其他类型插件定义

---

## 阶段3.5：核心机制测试

- [ ] `npm run test` 可运行
- [ ] 条件解析全覆盖
- [ ] 事件总线全覆盖
- [ ] 模板继承全覆盖
- [ ] 实体系统全覆盖
- [ ] 每个插件有启用/禁用隔离测试
- [ ] 最小测试模组集成测试通过

---

## 阶段5：基础UI框架（自查项）

### 响应式布局
- [ ] PC 端左右分栏正常
- [ ] 移动端 (<768px) 上下堆叠
- [ ] 按钮最小 44px 触摸目标

### UI插槽
- [ ] 预定义插槽名全部就位（location-panel、character-list、command-bar、log-panel、main-menu）
- [ ] `<component :is="item.component" />` 动态渲染正常
- [ ] 优先级排序正确
- [ ] **静态声明**（plugin.toml [ui]段）和**动态注册**（ctx.ui.registerSlot）两种机制都可用
- [ ] 条件函数实时求值，不满足不渲染
- [ ] 插件 onDisable 时自动移除注册

### 主题系统
- [ ] CSS 变量注入/卸载机制可用
- [ ] 换主题所有组件跟随变色
- [ ] **核心游戏界面无 Naive UI 组件**
- [ ] 辅助界面 Naive UI 组件走主题变量覆写
- [ ] 所有 CSS 变量名遵循 AGENTS.md 第26节的必需变量清单（colors/typography/spacing）
- [ ] 模组可加自定义变量且不互相冲突

### 通用组件
- [ ] 属性条（进度条）
- [ ] 角色头像
- [ ] 消息日志（支持滚动+不同类型消息）
- [ ] 统一样式按钮

---

## 阶段6-7：地图+角色+口上（自查项）

### 地图
- [ ] 平铺加载 + parent 字段定义层级（非硬编码三级）
- [ ] exit 校验：引用不存在的 target → 报错
- [ ] 不可达地点 → 警告
- [ ] type 字段用户自定义，引擎不预设
- [ ] tags 控制按钮显示/隐藏
- [ ] 移动触发 location:enter/location:leave 事件

### 角色
- [ ] 三级体系全部可用（named/、roster.toml、npc.toml）
- [ ] 模板继承正确合并属性
- [ ] 计算属性 compute 生效
- [ ] 属性绑定（bindings）读写正确

### 口上
- [ ] 按场景分类（greet/hurt/battle_start等）
- [ ] 条件触发正确
- [ ] 多条同条件台词随机播放
- [ ] 模板继承：子角色没写的场景用父模板

---

## 阶段8-10：背包+战斗+任务（自查项）

### 背包
- [ ] 物品添加/移除/使用逻辑正确
- [ ] 堆叠规则正确
- [ ] 物品效果正确执行
- [ ] 物品数量可作为条件字段

### 效果系统
- [ ] 效果类型可扩展注册
- [ ] 标准效果格式统一
- [ ] 效果执行触发标准事件

### 战斗
- [ ] 回合制逻辑正确
- [ ] 技能系统正确（伤害/消耗/效果）
- [ ] 技能解锁层级正确（如九阴真经6级→九阴神爪）
- [ ] 标准事件发出正确（combat:start/turn/end）
- [ ] 战斗口上正确触发
- [ ] **禁用战斗插件后其他功能正常**

### 任务
- [ ] 节点式分支剧情正确
- [ ] 条件触发正确
- [ ] 效果奖励走统一效果系统
- [ ] 多任务并行不冲突

---

## 阶段11：调试工具（自查项）

### @ 命令调试
- [ ] `@` 前缀命令输出到叙事日志
- [ ] `@additem 物品ID 数量` — 添加物品
- [ ] `@setattr 属性名 值` — 修改属性
- [ ] `@teleport 地点ID` — 传送到地点
- [ ] `@spawn 角色模板ID 地点ID` — 生成角色
- [ ] `@errors` — 查看所有错误
- [ ] 浏览器 console 可用于调试
- [ ] `@removeitem 物品ID 数量` — 移除物品
- [ ] `@setattr 属性路径 值` — 设置属性
- [ ] `@moveto 地点ID` — 传送
- [ ] `@startquest 任务ID` — 开始任务
- [ ] `@help` — 查看指令列表
- [ ] 插件可注册自定义指令

---

## 阶段12：插件化架构完善 + 示例模组（自查项）

### 插件化闭环
- [ ] 所有插件通过 plugin.toml 声明 UI 入口，引擎自动挂载
- [ ] 单个插件报错 → 禁用该插件 + 弹红色警告，其他插件正常
- [ ] 插件版本号 semver 校验通过/失败都精准报错
- [ ] 插件依赖检查：缺失依赖阻止加载
- [ ] 插件 extends 链接正确建立

### 示例模组
- [ ] 完整模组可跑通核心循环：移动→对话→接任务→战斗→交任务→获得奖励
- [ ] 包含：3级角色模板、≥3可交互角色、≥5地点、基础物品、≥1主线+2支线、基础战斗
- [ ] 换模组正常（另做一个2角色+1地点的哈利波特迷你测试模组）

---

## 阶段13：存档系统 + 多端适配 + PWA（自查项）

### 存档
- [ ] 多存档位（≥5个）
- [ ] 手动存档 + 关键节点自动存档
- [ ] 插件数据命名空间隔离
- [ ] 存档迁移链正确执行
- [ ] 存档导入/导出 JSON

### 移动端
- [ ] 所有按钮 ≥ 44px 触摸目标
- [ ] 字体≥16px，移动端不缩放
- [ ] 输入框弹出键盘不遮挡布局
- [ ] 滚动体验流畅
- [ ] 刘海屏安全区适配

### PWA
- [ ] 可添加到桌面
- [ ] 离线运行核心功能
- [ ] 启动图标正常

---

## 阶段14（可选）：沙箱脚本 + 角色AI

### 沙箱
- [ ] 脚本无法访问 DOM/全局对象/文件系统
- [ ] 5秒超时自动终止
- [ ] 脚本报错仅影响自身功能，不崩溃全局

### 角色AI
- [ ] 角色按 behavior 自主移动
- [ ] 时间规则（time_rules）正常触发
- [ ] 结伴倾向（follow_bias）正常
- [ ] 活跃度降频正常

---

## 阶段15（可选）：LLM口上

### LLM
- [ ] API key 通过环境变量/设置面板输入
- [ ] 流式输出正常
- [ ] 上下文历史正确截断
- [ ] token 计数/计费显示
- [ ] LLM 不可用时的降级策略（通用口上兜底）

---

## 跨阶段持续检查项

### 时间系统（实现后持续检查）
- [ ] `advanceTime(分钟数)` 正确推进 clock
- [ ] 到达 24:00 自动跳天 + 触发 `game:new_day`
- [ ] 行动耗时修正机制可用（读角色属性/物品后返回修正耗时）
- [ ] 睡眠行动跳转到第二天设定时刻

### 性能（在AI系统实现时检查）
- [ ] NPC AI只处理玩家当前地+相邻地
- [ ] 分帧调度：每帧 ≤ 10ms
- [ ] 活跃度 < 0.3 降频

### 存档（在存档系统时检查）
- [ ] 多存档位正常
- [ ] 命名空间隔离（模组/插件独立）
- [ ] 迁移链正确执行
- [ ] NPC加载：存档权威模型（存档恢复，模板不覆盖存档）

### 移动端（持续检查）
- [ ] <768px 布局正确
- [ ] 按钮 ≥ 44px
- [ ] 输入框不被键盘遮挡
- [ ] 滚动流畅

---

## 自审命令参考

开发过程中应定期运行：

```bash
npm run test          # 全量测试
npm run test:unit     # 单元测试
npm run test:plugins  # 插件隔离测试
npm run validate      # TOML文件全量校验（实现后）
npm run dev           # 启动开发服务器验证
```

---

## 代码与设计一致性验证（每个阶段完成时必须跑）

**机制**：以下验证确保写出来的代码与 AGENTS.md 设计严格对应。**每个阶段commit前必须全部通过**。

### 类型一致性
- [ ] `src/core/types.ts` 文件存在，内含 AGENTS.md 中定义的所有接口（PluginContext / GameContext / GameTimeData / LocationData / EntityData / UISlotItem）
- [ ] 所有插件入口函数签名为 `export function onEnable(ctx: PluginContext)`
- [ ] 所有通过 `ctx.api.call` 传递的命名空间和方法名在 AGENTS.md 第20节有定义
- [ ] `LocationData.exits` 含 `time_cost?` 字段（对应第22节移动耗时）

### 目录结构一致性
- [ ] `src/core/` 含 AGENTS.md 列出的全部 10 个文件（mod-loader / plugin-manager / entity-system / template / binding-resolver / condition / event-bus / save-system / game-context / api / condition-registry / error-reporter）
- [ ] `src/plugins/` 含 AGENTS.md 列出的全部插件目录（含 status-system, ability-progression）
- [ ] `src/ui/` 含 AGENTS.md 列出的全部子目录（layout / components / views / slots，无 dev-panel）
- [ ] 不存在 AGENTS.md 未列出的核心文件或插件（防止Agent随意发明新模块）

### 事件一致性
- [ ] 所有标准领域（combat/item/location/dialogue/quest/character/game/save）的事件名遵循 `领域:动作` 格式
- [ ] 事件名与 AGENTS.md 第6节的最小事件集一致（如 `item:added` 而非 `add_item`）
- [ ] 非标准事件皆含插件前缀

### 属性绑定一致性
- [ ] 所有插件代码中对属性的读写走 `ctx.api.call('engine', 'bindings.get/set', ...)`
- [ ] 无硬编码属性路径（如 `entity.base.气血`、`player.base.hp`）
- [ ] `bindings.toml` 中的映射对应 `plugin.toml` 的 `required_attributes` 键名

### 条件系统一致性
- [ ] 条件表达式只含 `> < >= <= == != && || !` 和括号
- [ ] 无条件字段硬编码在代码中（字段全部来自第21节的动态收集）
- [ ] `可用条件属性手册.md` 在项目根目录自动生成

### 架构层一致性
- [ ] `src/core/` 代码不含任何具体属性名（如 气血、内力、魔法值、生命值）
- [ ] `src/core/` 代码不含任何具体玩法逻辑（战斗公式、任务节点、物品效果等）
- [ ] `src/core/` 代码不含任何具体世界观设定（门派名、地点名、角色名等）
- [ ] 插件之间无直接 import（extends 走 `ctx.parent.api`）

### 运行验证命令
```bash
npm run lint            # ESLint / Prettier（如果配置了）
npm run typecheck       # TypeScript 类型检查
npm run validate        # TOML 全量校验
npm run test            # 全量测试必须通过
npx tsc --noEmit        # 确保类型定义与实现一致
```

---

## 已知陷阱（实现时容易出错）

| 陷阱 | 预防 |
|------|------|
| 条件路径写死 `player.base.hp` 而非走绑定 | 代码里只用 `ctx.api.call('engine', 'bindings.get', ...)` |
| 子插件直接 import 父插件文件 | 走 `ctx.parent.api` |
| TOML 嵌套数组写错格式 | 运行 `npm run validate` |
| 事件名不遵循 `domain:action` | 事件总线 emit 时校验格式 |
| 地图目录按三级分而非平铺 | 新建地图一律放 `mods/[模组]/maps/locations/` |
| 角色不分三级全建文件夹 | 龙套用 roster.toml，路人用 npc.toml |
| 静默 catch 不报错误 | 错误处理必须走 `errorReporter.report()` |
| CSS 写死颜色而非走变量 | 代码 review 时检查所有 `color:` / `background:` |
| 条件字段写死在代码里而非动态收集 | 字段来源只能是 plugin.toml 的 condition_fields + attributes.toml |
| 时间用 setInterval 而非行动驱动 | 时间只通过 `advanceTime(分钟)` 推进，不走真实时钟 |
| 忘记错误报文件名+行号 | `errorReporter.report()` 必须含 file + line |
| 硬编码主题变量名而非走第26节清单 | 只用 `--color-primary` 等标准变量，不发明新名 |

---

## 后续 TODO（非 MVP，押后实现）

### H 子系统（指引在 docs/superpowers/plans/phase-h-implementation-guide/）
- [ ] h-hypnosis 催眠系统（日常偷窃/H 催眠/同意绕过）
- [ ] h-time-stop 时停系统（绝顶累积/理智消耗/搬运）
- [ ] h-hidden 隐奸系统（4级隐匿/发现度）
- [ ] h-group 群交系统（模板/NPC AI/多人修正）
- [ ] h-confinement 监禁调教系统
- [ ] h-bondage 紧缚系统（3级紧缚/实时效果）
- [ ] h-aromatherapy 香薰疗愈（8种每日buff）
- [ ] 女儿成长→自订角色入口（h-pregnancy 扩展）

### H 深化
- [ ] 动态体位切换（15 体位 × 5 部位）
- [ ] NPC H AI（H 内自动行动）
- [ ] 二段行为（绝顶/射精后连锁）
- [ ] 宝珠系统（24 种宝珠睡眠结算）
- [ ] 口上三层加权随机（通用/角色/特殊情境）
- [ ] 纸娃娃地文（占位符替换 {vagina} 等）

### 引擎深化
- [ ] LLM 口上（流式/上下文/token/降级）
- [ ] 天赋/套装钩子式效果（先手/反击/反伤/连击等，需沙箱）
- [ ] combat-wuxia 公式 mod override 完整机制
- [ ] 战斗外精确分钟级 tick
- [ ] NPC 队友 AI 优化
- [ ] 限时/重复/日常任务
- [ ] 日志搜索/过滤
- [ ] 多图立绘 variants
- [ ] 地图层级文档自动生成
- [ ] 自动化脚本/宏（字符串 ID 链式执行）
- [ ] onDisable/onUnload 插件生命周期
- [ ] semver 版本校验
- [ ] required_attributes 继承
- [ ] 标准事件契约完整发出
- [ ] getDefaultValue 类型感知默认值

### 侧栏/选项 TODO
- [ ] 选项面板（开关 cheatCommands、@命令、主题切换、字体等）
- [ ] 现代主题：侧栏 parameter 区 + 主体 parameter 区各自独立开关
- [ ] 侧栏三条杠手柄承载更多简要信息
- [ ] 目标选择菜单（仙剑式：选技能→选目标→执行）
- [ ] 战斗 UI 美化（仙剑式布局：左上敌人+右下队友+左下指令轮盘）
- [ ] 全体技能/回复/buff（选中任一目标即对全体生效）
- [ ] 战斗中不可选中队友为攻击目标
- [ ] HP=0 后角色处理（从地点移除/留 1HP/回营地）
