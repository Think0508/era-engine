# lick_anal 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：high_1 → 条件：premise(high_1)
variable = "lick_anal"
description = "舔肛指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 高权重基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "{player.name}用双手轻轻掰开{character.name}的屁股，舌头强硬的钻入{character.name}的菊穴，随后顺着后穴的纹理舔舐了起来。"

[[entries]]
conditions = "premise(high_1)"
context = "舔肛指令通用口上 1"