# hidden_sex_end 指令默认通用口上（插件默认层，Layer 1）
variable = "hidden_sex_end"
description = "结束隐奸指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"
# ⚠️ 骨架：不搬运 erArk 原文；1 示例 + 1 占位。
[[entries]]
context = "{player.name}结束了这次隐奸。"

[[entries]]
context = "结束隐奸指令通用口上 1"