# change_womb_sex_position 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：insert/子宫链（high_1 示例）→ 条件：premise(high_1)
variable = "change_womb_sex_position"
description = "换子宫姦体位指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "{player.name}调整姿势，抵入{character.name}的子宫。"

[[entries]]
conditions = "premise(high_1)"
context = "change_womb_sex_position指令通用口上 1"
