# back_lay_sex 指令默认通用口上（插件默认层，Layer 1）
# ⚠️ 骨架：不搬运 erArk 长文本；保留 1 条简短示例 + 1 条占位符。
# erArk 原前提：insert/V 链（high_1 示例）→ 条件：premise(high_1)
variable = "back_lay_sex"
description = "背面卧位指令默认通用口上（角色通用口上 Layer 1 兜底，mod 可覆盖）"

# ── 基础组 ──────────────────────────────────────────────
[[entries]]
conditions = "premise(high_1)"
context = "从背后环住侧卧的{character.name}，{player.name}轻轻进入。"

[[entries]]
conditions = "premise(high_1)"
context = "back_lay_sex指令通用口上 1"
